2026-03-25T00:00:00Z | Created CLAUDE.md with prompt logging instructions and initialized prompts.txt.
2026-03-25T00:01:00Z | Set up base project structure: rewrote README.md, created /docs/API_DECISIONS.md (empty), and created /collections/ folder.
2026-03-25T00:02:00Z | Created Postman collection JSON with Auth folder: 5 POST /auth/login cases (valid, wrong password, non-existent user, missing username, missing password) and 3 GET /auth/me cases (valid token, invalid token, no token). Each request has test scripts for status code, response structure, and key fields. Valid login saves accessToken to {{token}} env var.
2026-03-25T00:03:00Z | Updated "GET /auth/me — invalid token" test to expect 500 instead of 401 (API bug: DummyJSON throws 500 on malformed tokens). Added inline comment in test script. Documented the bug in docs/API_DECISIONS.md.
2026-03-25T00:04:00Z | Added Products folder to Postman collection with 7 requests: GET all (with pagination validation), GET by ID 1, GET non-existent ID 99999, POST /products/add, PUT /products/1, PATCH /products/1, DELETE /products/1. Each has status, structure, and field-level test scripts. POST/PUT assert sent fields are reflected in response.
2026-03-25T00:05:00Z | Added Search & Filtering folder with 7 requests: search with results (q=phone) including relevance check, search with no results (q=xyznotfound), empty query (q=), GET categories, GET by category/smartphones (all results match category), first page (limit=5&skip=0) saving IDs to env, second page (limit=5&skip=5) asserting no overlap with first page.
2026-03-25T00:06:00Z | Added Error Handling folder with 7 requests covering invalid ID (0), negative ID (-1), string ID (abc), empty POST body, malformed JSON body, non-existent user (99999), non-existent product DELETE (99999). Each checks status code, message field presence, and absence of sensitive fields (stack, trace, password).
2026-03-25T00:07:00Z | Updated "POST add product — empty body" test to expect 201 instead of 4xx (API issue: DummyJSON accepts empty body and creates a product with no field validation). Added inline comment. Documented in docs/API_DECISIONS.md.
